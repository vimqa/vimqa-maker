spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/qa_dataset?createDatabaseIfNotExist=true&useSSL=false
  jpa:
    #    show-sql: true
    # Drop and create new tables (create, create-drop, validate, update)
    # Only for testing purpose - comment this in production
    #    hibernate:
    #      ddl-auto: update
    database: mysql
#server:
#  servlet:
#    context-path: /api


application:
  wiki-language-code: vi
